@inherits Umbraco.Web.Mvc.UmbracoViewPage<Umbraco.Web.PublishedModels.Home>

@*@inherits Umbraco.Web.Mvc.UmbracoViewPage<Home>
    @using ContentModels = Umbraco.Web.PublishedModels;*@

@{
    Layout = "master.cshtml";
}
@* we are using ModelsBuilder *@

@*<div style="color: @Model.ColourName">
        <h1>@Model.Title</h1>
        @Model.MainContent
        <p>Your favourite colour is @Model.ColourName</p>
    </div>

    @if (Model.ShowImage)
    {
        <img src="/media/p2ufr25b/yorkie_umbraco.jpg" title="dog picture"/>
    }*@


<!-- Page Header -->
<header class="masthead" style="background-image: url('img/home-bg.jpg')">
    <div class="overlay"></div>
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
                <div class="site-heading">
                    @*<h1>@(!string.IsNullOrEmpty(Model.PageTitle) ? Model.PageTitle : Model.Name)</h1>*@
                    <h1>@Model.Name</h1>
                    <span class="subheading">A Blog Theme by Start Bootstrap</span>
                </div>
            </div>
        </div>
    </div>
</header>

@Html.Partial("~/Views/Partials/latestArticles.cshtml")